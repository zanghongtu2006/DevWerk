[pytest]
minversion = 8.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Strict coverage reporting.
addopts = -ra -q --tb=short

# Set APP_ENV=test for all test runs so no accidental real API calls are made.
env =
    APP_ENV=test

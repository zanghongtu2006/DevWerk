# Inspect Browser

Request browser diagnostics for the current project UI.

Expected agent behavior:

- Ask the connected capability provider for Playwright or CDP evidence.
- Capture screenshot, console errors, and network failures when available.
- Use findings to update the current workflow phase output.
